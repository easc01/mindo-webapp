enum APP_ENVIRONMENT {
  DEVELOPMENT = 'dev',
  PRODUCTION = 'prod',
  QA = 'qa',
}

enum SORT_ORDER {
  ASC = 'ASC',
  DESC = 'DESC',
}

enum ToastVariant {
  DEFAULT = 'default',
  WARNING = 'warning',
  SUCCESS = 'success',
  ERROR = 'error',
}


export { APP_ENVIRONMENT, SORT_ORDER, ToastVariant }
